<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray_100">

    <!-- Header Sederhana -->
    <LinearLayout
        android:id="@+id/header_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/emerald_700"
        android:padding="24dp"
        android:orientation="vertical">

        <!-- Tambahkan ID agar judul bisa diubah via Java -->
        <TextView
            android:id="@+id/tv_header_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Panduan Sholat"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/white"/>

        <!-- Tambahkan ID agar deskripsi bisa diubah via Java -->
        <TextView
            android:id="@+id/tv_header_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pelajari gerakan dan bacaan sholat fardhu."
            android:textColor="@color/emerald_50"
            android:textSize="12sp"
            android:layout_marginTop="4dp"/>
    </LinearLayout>

    <!-- RecyclerView: Panduan Sholat -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_guide"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header_view"
        android:layout_above="@id/bottom_navigation"
        android:clipToPadding="false"
        android:paddingBottom="20dp" />

    <!-- RecyclerView: Kumpulan Doa (Tambahan Baru) -->
    <!-- Default visibility gone, akan dimunculkan saat tab Doa diklik -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_doa"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header_view"
        android:layout_above="@id/bottom_navigation"
        android:clipToPadding="false"
        android:paddingBottom="20dp"
        android:visibility="gone" />

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/white"
        app:itemIconTint="@color/emerald_700"
        app:itemTextColor="@color/emerald_700"
        app:menu="@menu/bottom_nav_menu" />

</RelativeLayout>